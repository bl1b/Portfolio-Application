<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Portfolio_Title" parent="TextAppearance.AppCompat.Headline">
        <item name="android:gravity">center</item>
        <item name="android:color">@color/material_blue_grey_800</item>
        <item name="android:layout_marginBottom">32dp</item>
    </style>

    <style name="Portfolio_Button" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/button_material_light</item>
        <item name="android:background">@color/material_blue_grey_900</item>
    </style>
</resources>
